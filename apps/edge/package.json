{
  "name": "@galaxy/edge",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node --env-file=../../.env.edge dist/main.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src test",
    "check": "pnpm lint && pnpm typecheck && pnpm test && pnpm build"
  },
  "dependencies": {
    "http-proxy": "1.18.1"
  },
  "devDependencies": {
    "@eslint/js": "9.39.1",
    "@types/http-proxy": "1.17.16",
    "@types/node": "20.19.24",
    "eslint": "9.39.1",
    "typescript": "5.9.3",
    "typescript-eslint": "8.46.3",
    "vitest": "3.2.6"
  }
}
